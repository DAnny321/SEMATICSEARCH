pageextension 80321 "GPT Item List Extension" extends "Item List"
{
    layout
    {
        addlast(Control1)
        {
            field("GPT Vector"; Rec."GPT Vector")
            {
                ApplicationArea = All;
                Caption = 'GPT Vector';
            }

            field("GPT Vector 1"; Rec."GPT Vector 1")
            {
                ApplicationArea = All;
                Caption = 'GPT Vector 1';
            }

            field("GPT Vector 2"; Rec."GPT Vector 2")
            {
                ApplicationArea = All;
                Caption = 'GPT Vector 2';
            }

            field("GPT Vector 3"; Rec."GPT Vector 3")
            {
                ApplicationArea = All;
                Caption = 'GPT Vector 3';
            }

            field("GPT Vector 4"; Rec."GPT Vector 4")
            {
                ApplicationArea = All;
                Caption = 'GPT Vector 4';
            }

            field("GPT Vector 5"; Rec."GPT Vector 5")
            {
                ApplicationArea = All;
                Caption = 'GPT Vector 5';
            }

            field("GPT Vector 6"; Rec."GPT Vector 6")
            {
                ApplicationArea = All;
                Caption = 'GPT Vector 6';
            }

            field("GPT Vector 7"; Rec."GPT Vector 7")
            {
                ApplicationArea = All;
                Caption = 'GPT Vector 7';
            }

            field("GPT Vector 8"; Rec."GPT Vector 8")
            {
                ApplicationArea = All;
                Caption = 'GPT Vector 8';
            }

            field("GPT Vector 9"; Rec."GPT Vector 9")
            {
                ApplicationArea = All;
                Caption = 'GPT Vector 9';
            }

            field("GPT Vector 10"; Rec."GPT Vector 10")
            {
                ApplicationArea = All;
                Caption = 'GPT Vector 10';
            }

            field("GPT Vector 11"; Rec."GPT Vector 11")
            {
                ApplicationArea = All;
                Caption = 'GPT Vector 11';
            }
        }
    }
}